#!/usr/bin/env bash
set -eoux pipefail

git config --global alias.co checkout
git config --global alias.st status
git config --global user.name "Shakir Shakiel"
git config --global user.email "shakirshakiel@gmail.com"
